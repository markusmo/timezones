test project
using multipolygons in a database, using a point as location, determine which timezone the point is located

requirements for python in requirements.txt
database: postgresql v10.1 and postgis